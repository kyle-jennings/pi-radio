# Bluetooth Configuration
# Replace the MAC address below with your actual Bluetooth device MAC address
# You can find your device's MAC address by running: bluetoothctl devices

MAC_ADDRESS=AA:BB:CC:DD:EE:FF

# Server Configuration for Deployment
# Used by deploy.sh script to copy files to Raspberry Pi
SERVER_HOST=192.168.0.1
SERVER_USER=pi
# SERVER_PATH=/home/pi/pi-radio  # Optional: defaults to /home/$SERVER_USER/pi-radio

# Example of a real MAC address:
# MAC_ADDRESS=AC:12:2F:89:46:2B
